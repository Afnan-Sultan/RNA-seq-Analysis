  * This folder contains different lists for accession numbers with the code to download and storing these data in the appropriate directories. 
  * if you are going to keep the folder on desktop, then replace $username in .sh file with the PC username. if not, then replace $username with the PC-username/path-to-folder
  * The resulted folders will be structured as follows
      * This directory which will contain the code file, a directory contatining the accession lists and a directory named RNA-seq_data
      * RNA-seq_data folder will contain two folders, ribo-depleted and poly-A
      * each folder of these two will contain four folders for each sample in the paper. 
      
